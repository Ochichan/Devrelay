# DevRelay VS Code Extension

This package is the first editor-context slice for DevRelay. It starts with a
small local-agent bridge, status bar state, and command registration. Context
capture and restore stay behind later M8 checklist items.

```bash
npm install
npm run check
```
