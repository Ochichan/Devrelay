{
  "name": "devrelay-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ui": "node scripts/prepare-ui.mjs && npm run check:ui",
    "check:ui": "node scripts/check-ui-source.mjs && node scripts/check-ui-state-authority.mjs && node scripts/check-feature-status.mjs && node scripts/check-event-bridge.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:dmg": "tauri build --bundles app,dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.3"
  }
}
